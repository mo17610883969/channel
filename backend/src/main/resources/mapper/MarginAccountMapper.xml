<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.channelmargin.mapper.MarginAccountMapper">

    <update id="updateMarginTransitUseBalanceById">
        UPDATE margin_account
        SET margin_transit_use_balance = margin_transit_use_balance + #{amount}
        WHERE id = #{id}
    </update>

    <update id="updateSubMarginTransitUseBalanceById">
        UPDATE margin_account
        SET margin_transit_use_balance = margin_transit_use_balance - #{amount}
        WHERE id = #{id}
        AND margin_transit_use_balance >= #{amount}
    </update>

</mapper>
